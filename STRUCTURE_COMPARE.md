# 카피바라고 로그 vs chatsystem — 구조 동일성 판정 (확정)

> 세션 판정 수락 기록. 근거: HANDOFF(영상 3편 RE) + 코드/CSV. Notion/공개 위키는 모험 로그 본문 없음.

## 판정 (확정)

**원작 하단 로그의 이벤트 분류·연출 골격은 맞게 리버스한 데이터 엔진**이다.  
표현·스키마는 **동일 계열**, 발생 규칙·전투·일부 데이터 연결은 **프로토 단순화/미확정**.

| # | 질문 | 답 |
|---|------|-----|
| 1 | 표현 모델(피드·배너·칩) 동일 계열? | **예** |
| 2 | 콘텐츠 스키마(텍스트풀 FK·등급·효과 SSoT)? | **대체로 예** |
| 3 | 발생 규칙(전투 타이밍 등) 완전 동일? | **부분 아니오** |
| 4 | 원작 클라이언트 1:1 복제? | **아니오** (동등 모델) |

## 영상 RE 대조표 (추가 스크린샷 대기 항목)

| 항목 | 원작 정황 (영상 2·3) | chatsystem (이 세션 후) | 신뢰도 | 추가 촬영 필요 |
|------|---------------------|-------------------------|--------|----------------|
| 전투 일차 6·8·9·11·15·19 | 양편 모두 전투 몰림 → 고정 배치 정황 | `combat_day_schedule.csv`로 강제 전투 반영. 그 외 일차는 phase 확률 | 중 | 4편째로 고정 vs 확률 확정 |
| 미니게임 "행운의 보물"(골드 3릴) | 영상 명칭 | `mg_slot`=신화 스킬 룰렛(SLOT). 게이지 대박→`mg_slot`. `mg_square`=행운의 룰렛 | 낮 | 룰렛 UI 스크린샷으로 명칭·보상 대조 |
| LV3 스킬 등급 | 영상2 신화 / 영상3 일반 엇갈림 | `levelup_config` LV3=`일반` (잠정) | 낮 | LV3 스킬 선택 화면 캡처 |
| BRANCH_ROUTE 용병 영지 | 스키마상 분기 경로 | `LOCATION:loc_mercenary` 브랜치로 연결 | 중 | 원작에서 용병 영지 진입 경로 |
| 중박 게이지 보상 | 불명 | `gz_mid.reward_minigame_id` 비어 있음 | 낮 | 중박 게이지 풀 시 화면 |

## 이 세션에서 맞춘 구조 갭

1. 전투 고정일차 → CSV 스케줄
2. 무등급 효과 하드코딩 → `ungraded_pool.csv`
3. `BRANCH_ROUTE` → 분기 effect `LOCATION:` 연결
