# 다음 AI 붙여넣기 프롬프트 — 필러·시나리오·노드 동선

> 작성: 2026-08-09
> 이 파일 전체를 새 세션에 **그대로 붙여넣으면** 된다.
> 상세 인수인계: [`docs/gdd/44_인수인계_필러시나리오.md`](docs/gdd/44_인수인계_필러시나리오.md)

---

## ▼ 여기서부터 복사 ▼

작업 루트는 `C:\xoox` 다. **로컬 Agent만** 쓴다(Cloud Agent 금지).

### 네가 맡은 범위

**필러(카드 로그)의 종류 · 시나리오 과정 · 노드 동선.**

맡지 않는 것 — 손대기 전에 반드시 물어봐라:
- **디자인**(색·여백·모양·아트) — 별도 담당자가 있다
- **탐방 안개·가시성 렌더**(`explore.ts`의 fog 로직) — 별도 담당자가 있다

### 먼저 읽을 것 (순서대로)

1. `docs/gdd/44_인수인계_필러시나리오.md` ← **입구. 이것부터.**
2. `docs/gdd/38_시나리오_작성룰.md` — 저작 규칙 5개
3. `docs/gdd/39_게임시스템_확정.md` — 60일 한 판·실패·능력 2층
4. `docs/gdd/41_섬여정_갈래설계.md` — 3×3 루트·정화도·구조 밸런스
5. `docs/gdd/43_옛루프_교체계획.md` — 무엇을 왜 걷어냈나

### 이 게임이 뭔지 한 줄

> 방랑자가 **신비의 섬**에 내려 **60일** 동안 오염을 정화하고 반려동물을 구조한다.
> 배가 60일 뒤에 온다. 그 전에 쓰러지면 **구조한 아이를 전부 잃는다.**
> 카피바라고의 **간소화**(버튼 하나로 진행) + Cozy Grove의 **정화**(색이 돌아옴)를 섞은 로그라이크.

### 절대 규칙 4개

| | |
|---|---|
| **R1** | **하루 = 노드 하나.** 섹터 이동도 하루. 예외 없음 |
| **R2** | **사건은 날짜가 아니라 노드에 붙는다.** `day 30 = 보스` ❌ / `s2_boss 도착 = 보스` ✅ |
| **R3** | **정화가 지역을 연다.** 정화가 되어야 그 지역에 뭐가 있는지 안다. 정화 미션 **도중에** 행운의 상자·룰렛이 터진다 |
| **R4** | **순서는 고정, 안은 랜덤.** 모든 걸 갈림길로 만들면 안 된다. 랜덤이 반드시 필요하다 |

### 섹터 사이클 (SSoT: `data/sector_beat_config.csv`)

```
B1  오염 발견        BLIGHT                              1
B2a 촉매 찾기        CATALYST                            1
B2b 미션 도중        DAILY|MINIGAME                      1~2   ← 여기서 행운이 터진다
B3  정화 완료        BLIGHT_APPLY (B1과 같은 자리)        1
    ★ 여기서 지역이 드러난다 ★
B4  지역 탐방        MEMORY|LOCATION|REST|MEMO|NPC       1~3
B5  스킬             SKILL                               1~2
B6  조우             RESCUE                              1
B7  전투             COMBAT                              1~3
B8  다음 지역        MINIBOSS|BOSS                       1  (갈림길)
```

랜덤 3층: **L1 대상**(어느 노드) · **L2 횟수**(min~max) · **L3 길 판정**(등급→갈림길 또는 자동 랜덤)

### 길 판정 (`path_judgment_config.csv`)

등급이 곧 이동 모드다. 섹터 끝 + B3/B7 후 `path_judge_mid_chance_pct%`.
대박→갈림길 · 중박→갈림길(대가) · 보너스→자동 안전 · 운빨→자동 험함. 동시에 등급 카드·게이지.

### 하면 안 되는 것

1. **B2a와 B2b를 합치지 마라** — 랜덤이 `DAILY`만 골라 촉매를 못 줍고 B3 정화가 실패한다(실제 버그)
2. **가시성 판정을 `main.ts`에 복제하지 마라** — `explore.isNodeRevealed(npcId)`를 쓴다
3. **프로토(`*-proto.html`)의 CSS를 본게임에 옮기지 마라** — 구조 확인용 임시다
4. **코드에 ID·임계값을 박지 마라** — `dialogue_config.trigger`, `combat_tuning` 키를 쓴다
5. **`rollIsCombat`/`rollIsRescue`/`rollContentType`를 되살리지 마라** — 노드가 대체했다
6. 큰 작업 전 `backups/backup_YYYYMMDD_HHMMSS` 생성
7. **추측하지 말고 물어봐라.** 지시자는 프로그래머가 아니다. 용어가 애매하면 먼저 확인한다
8. **질문은 한 번에 하나씩.** 여러 개를 나열하면 답하기 어렵다

### 실행

```bash
npm run dev          # 본게임
npm run dev:scn      # 시나리오 체인 툴
npm run dev:sector   # 섹터 배치 에디터 (다른 담당)
npm run dev:layout   # 레이아웃 에디터 (다른 담당)
npm run dev:kit      # 아트 무대 툴 (다른 담당)
```

콘솔 훅: `__voyage()` `__setDay(n)` `__endVoyage()` `__failRun()` `__explore()` `__islandWarp('i11')`

### 네가 쓸 툴 3개

| 툴 | 명령 | 무엇에 쓰나 |
|---|---|---|
| **시나리오 체인 툴** | `npm run dev:scn` | 체인 저작 · 규칙 자동 검증 · **루트별 60일 통독** · 비트시트 내보내기. 갈래가 수십 개라 사람이 못 외우니 **툴이 대신 들고 있다** |
| **맵·이동 프로토** | `map-proto.html` | 3×3 섬에서 캐릭터가 어떻게 움직이는지 코드 없이 확인 |
| **필러 프로토 v2** | `filler-proto.html` | **카드 13종을 한 화면에서 다 봄** · 60일 갈래 통주행 · 판 비교표 |

> 세 툴 모두 **본게임과 독립**이다. 프로토의 CSS는 디자인 시안이 아니다.
> `scenario-tool`의 루트 계산은 **54노드 시절 기준**이라 지금(94노드) 재검산이 필요하다.

### 지금 상태

- 옛 주사위 루프 **제거 완료** — 「다음날」이 비트 사이클대로 노드를 밟는다
- 노드 **94개** (`RESCUE` 22 · `DAILY` 11 · `SKILL` 7 신규)
- 필러 카드 **13종** (감정 톤 sad/joy · 방랑자 독백 · 리더 채팅 · 장 헤더 · 전투 요약 추가)
- 한 섹터 사이클 B1~B7 **실플레이 확인**, 옛 문구 0건, `tsc -b` 통과

### 바로 할 일 (우선순위)

1. **B8 갈림길 실플레이 검증** — 코드는 있으나 B7까지만 도달했다
2. **`devilModalPanel`이 빈 채로 남는 버그** — `showBranchPrompt`의 PREVIEW_DECLINE 경로
3. **60일 완주 검증** — 5섹터 통주행 · 옛 문구 0 확인
4. `day_event` 5건을 노드로 이관하고 `getDayEvent` 제거 (R2 위반 잔재)
5. 94노드 기준 60일 예산 재검산 (사이클 7~12걸음 × 5섹터 + 이동 6 = 41~66일)

나머지는 `docs/gdd/44` §9에 10건이 우선순위대로 있다.

### 첫 응답으로 해줄 것

1. `docs/gdd/44_인수인계_필러시나리오.md`를 읽고 **네 말로 요약**해라 (내가 맞는지 확인한다)
2. 위 「바로 할 일」 1번부터 시작할지, 다른 걸 먼저 할지 **하나만** 물어라

## ▲ 여기까지 복사 ▲
